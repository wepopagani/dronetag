import { PublicDroneChrome } from '@/components/layout/PublicDroneChrome';

export default function PublicDroneLayout({ children }: { children: React.ReactNode }) {
  return <PublicDroneChrome>{children}</PublicDroneChrome>;
}
